import Sidebar from '../components/Sidebar';
import ChatArea from '../components/ChatArea';

const Home = () => {
    return (
        <>
            <Sidebar />
            <ChatArea />
        </>
    );
};

export default Home;
